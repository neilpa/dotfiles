alias reload!='. ~/.zshrc'
alias errcho='>&2 echo'

# From the zmv usage
# If you combine this with noglob, you don't even need to quote the arguments
alias mmv='noglob zmv -W'
# mmv *.c.orig orig/*.c" >&2

